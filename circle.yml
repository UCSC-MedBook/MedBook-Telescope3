machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    #TODO figure out how to pass tag in here
    - docker build -t medbook/telescope .
    - mkdir -p ~/docker; docker save medbook/telescope > ~/docker/image.tar

test:
  override:
    - echo "tests go here"

deployment:
  hub: 
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push medbook/telescope